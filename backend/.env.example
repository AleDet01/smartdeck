# Backend Environment Variables Configuration

# Node Environment
NODE_ENV=production # development | production | test

# Server Configuration
PORT=3000

# Database Configuration
MONGODB_URI=your_mongodb_connection_string_here
# Esempio: mongodb+srv://username:password@cluster.mongodb.net/smartdeck?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters
# IMPORTANTE: Genera un JWT_SECRET sicuro con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
ALLOW_ORIGINS=http://localhost:3001,http://localhost:3000
# In produzione: ALLOW_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate Limiting (opzionale, default in codice)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100

# Security (opzionale)
# SESSION_SECRET=your_session_secret_here
# COOKIE_DOMAIN=.yourdomain.com

# Logging (opzionale)
# LOG_LEVEL=info # debug | info | warn | error

# ==================================================
# ISTRUZIONI PER LA CONFIGURAZIONE
# ==================================================

# 1. Copia questo file in `.env` (non committare .env!)
# 2. Sostituisci tutti i valori placeholder con dati reali
# 3. In produzione, usa variabili d'ambiente del provider (Render, Heroku, etc.)
# 4. Non condividere mai il file .env o i suoi contenuti
# 5. Aggiungi .env al .gitignore

# ==================================================
# GENERAZIONE VALORI SICURI
# ==================================================

# JWT_SECRET (Node.js):
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# SESSION_SECRET (Node.js):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

# ==================================================
# SICUREZZA
# ==================================================

# - Mai committare .env nel repository
# - Usa secret manager in produzione (AWS Secrets Manager, Azure Key Vault, etc.)
# - Ruota le chiavi periodicamente (ogni 90 giorni)
# - Usa chiavi diverse per dev/staging/prod
# - Limita accesso alle variabili d'ambiente solo al personale autorizzato
